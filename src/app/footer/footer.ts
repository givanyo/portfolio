import { Component } from '@angular/core';
import { Button } from '../shared/button/button';

@Component({
  selector: 'app-footer',
  imports: [Button],
  templateUrl: './footer.html',
  styleUrl: './footer.css',
})
export class Footer {}
